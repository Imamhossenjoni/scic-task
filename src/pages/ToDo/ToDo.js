import React from 'react';
import TodoForm from './TodoForm';
import TodoList from './TodoList';

const ToDo = () => {
    return (
        <div>
            <TodoForm/>
            <TodoList/>
        </div>
    );
};

export default ToDo;